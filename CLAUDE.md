# Harness-Specific Note

Use `AGENTS.md` for the canonical project instructions.

This file exists only for tools that still look for `CLAUDE.md`; keep it as a pointer instead of duplicating project guidance.
